# ── 보안 (절대 커밋 금지) ──
core/_secret_key.py
tools/
license.dat

# ── 로컬 설정 / 세션 (실행 시 자동 생성) ──
app_config.json
data_paths.json
session.json
settings.json

# ── 사용자 생성물 ──
*.lorascape
gw_list.csv
nodelist_*.csv
results/

# ── 대용량 / 저작권 데이터 ──
data/

# ── 파이썬 캐시 / 가상환경 ──
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# ── PyInstaller 빌드 산출물 ──
build/
dist/
# LoRaPlanner.spec  ← 빌드 설정은 공유하려면 주석 유지(=올림)

# ── OS / 에디터 ──
.vscode/
.idea/
.DS_Store
Thumbs.db